spring:
  mail:
    password: springboot
    username: duke
    host: 127.0.0.1
    port: 3025 # default protocol port + 3000 as offset
    protocol: smtp
    test-connection: true



  datasource:
    driver-class-name: org.postgresql.Driver
    jdbcUrl: jdbc:postgresql://localhost:5437/registration_db?stringtype=unspecified
    username: postgres
    password: postgres




logging:
  level:
    org.springframework.boot: ERROR