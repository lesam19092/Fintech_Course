spring:
  liquibase:
    enabled: false
  application:
    name: FoodRu_MicroService
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    jdbcUrl: jdbc:postgresql://localhost:5432/food_db
    username: postgres
    password: postgres
    poolName: myPool
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.yandex.ru
    port: 587
    username: daniil.glovatsky@yandex.ru
    password: hngnlwgjftlauomk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  s3:
    bucketName: bucket-for-s3
    accessKeyId: YCAJE0sU1RpxZtayJhor-EAg3
    secretAccessKey: YCPGq37wBlm2LlC0k35_qwqncdf0RROs5FTtKAKo
  kafka:
    properties:
      sasl:
        jaas:
          config:
            org.apache.kafka.common.security.plain.PlainLoginModule required username=${kafka_username:'admin'} password=${kafka_password:'admin-secret'}; mechanism:
              PLAIN security:
                protocol: SASL_PLAINTEXT

server:
  port: 7070